// import { useFetch } from "../hooks/useFetch";
const HomePage = () => {
  // const { data: article, isPendeing: loading, error } = useFetch();
  return <div>HomePage</div>;
};

export default HomePage;
